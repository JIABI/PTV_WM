runs:
  - stage: train
    overrides: {domain: lic_boids, task: existence, trainer.epochs: 40, trainer.batch_size: 64, trainer.history_len: 16, trainer.pred_len: 12, domain.n_samples: 1024, domain.num_agents: 24}
  - stage: eval
    overrides: {domain: lic_boids, task: existence, evaluator: predictive}
