# Shared PTV configs

This folder contains shared framework-level configuration.

Current shared config sources:
- Atlas/fixed-interface smoke and paper configs in `configs/*.yaml` (legacy atlas core).
- Additional shared config utilities will be migrated here in subsequent passes.
