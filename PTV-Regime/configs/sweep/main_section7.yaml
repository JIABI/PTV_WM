runs:
  - stage: train
    overrides: {domain: eth_ucy, task: cross_domain, trainer.epochs: 60, trainer.batch_size: 64, trainer.history_len: 16, trainer.pred_len: 12}
  - stage: eval
    overrides: {domain: eth_ucy, task: cross_domain, evaluator: predictive}
  - stage: train
    overrides: {domain: sdd, task: cross_domain, trainer.epochs: 60, trainer.batch_size: 64, trainer.history_len: 16, trainer.pred_len: 12}
  - stage: eval
    overrides: {domain: sdd, task: cross_domain, evaluator: predictive}
  - stage: train
    overrides: {domain: interaction, task: cross_domain, trainer.epochs: 60, trainer.batch_size: 64, trainer.history_len: 16, trainer.pred_len: 12}
  - stage: eval
    overrides: {domain: interaction, task: cross_domain, evaluator: predictive}
